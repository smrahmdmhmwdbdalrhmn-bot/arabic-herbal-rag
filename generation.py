```python
# -*- coding: utf-8 -*-
"""
generation.py

خطوة التوليد لتطبيق Arabic Herbal RAG
باستخدام Groq API بدلًا من Ollama.
"""

from groq import Groq


# ============================================================
# إعدادات الموديل
# ============================================================

DEFAULT_MODEL = "openai/gpt-oss-20b"


# ============================================================
# التنبيه الطبي
# ============================================================

DISCLAIMER = (
    "هذه المعلومات مستقاة من مصدر مرجعي في الأعشاب الطبية، "
    "ولا تُغني عن استشارة طبيب مختص."
)


# ============================================================
# بناء الـ Prompt
# ============================================================

def build_strict_prompt(query, context_text):

    if not context_text or not context_text.strip():
        return None

    prompt = f"""
أنت مساعد متخصص في الأعشاب الطبية داخل موسوعة عربية.

مهمتك هي الإجابة على سؤال المستخدم اعتمادًا فقط على
المعلومات الموجودة في "السياق" المرفق.

قواعد صارمة يجب الالتزام بها:

1. استخدم فقط المعلومات الموجودة في السياق.
   ممنوع اختراع أو إضافة معلومات من خارج السياق.

2. إذا كان السياق لا يحتوي على معلومات كافية للإجابة
   على السؤال، اكتب بوضوح:

   "المعلومات المتاحة لا تكفي للإجابة على هذا السؤال بدقة."

3. إذا وجدت عشبة مصنفة [HIGH_RISK] في السياق،
   يجب ذكر التحذير المرتبط بها بوضوح.
   لا تحذف التحذير ولا تقلل من أهميته.

4. لا تقدم تشخيصًا طبيًا.

5. لا تقدم نصيحة طبية قاطعة مثل:
   "استخدم هذه العشبة"
   أو
   "لا تستخدم هذه العشبة".

   اعرض فقط ما ورد في المصدر، واذكر أن المعلومات
   لا تغني عن استشارة الطبيب.

6. لا تضف معلومات أو فوائد أو أضرار أو جرعات
   غير موجودة في السياق.

7. إذا كان السؤال عن فوائد عشبة معينة،
   اذكر فقط الفوائد الموجودة في السياق.

8. إذا كان السؤال عن الأضرار أو المخاطر،
   اذكر فقط المعلومات الموجودة في السياق.

9. إذا كان السؤال عن الأمان أو الاستخدام،
   اعتمد فقط على التصنيف والمعلومات الموجودة في السياق.

10. في نهاية الإجابة، اذكر أسماء الأعشاب التي اعتمدت عليها
    من السياق تحت عنوان:

    "المصادر:"

11. إذا لم توجد عشبة مرتبطة بالسؤال أو لم يكن السياق
    كافيًا للإجابة، لا تخترع مصدرًا.

12. اكتب الإجابة باللغة العربية بشكل واضح ومنظم ومختصر.

------------------------------------------------------------
السياق:
------------------------------------------------------------

{context_text}

------------------------------------------------------------
السؤال:
------------------------------------------------------------

{query}

------------------------------------------------------------
الإجابة:
------------------------------------------------------------
"""

    return prompt.strip()


# ============================================================
# توليد الإجابة باستخدام Groq
# ============================================================

def generate_answer(query, context_text, api_key, model=DEFAULT_MODEL):

    # بناء الـ prompt
    prompt = build_strict_prompt(
        query=query,
        context_text=context_text
    )

    # في حالة عدم وجود سياق
    if prompt is None:
        return {
            "answer": (
                "المعلومات المتاحة لا تكفي للإجابة على هذا السؤال "
                "بدقة — لم يتم العثور على معلومات مرتبطة بالسؤال "
                "في الموسوعة."
            ),
            "disclaimer": DISCLAIMER
        }

    # إنشاء Groq Client
    client = Groq(api_key=api_key)

    # إرسال الطلب إلى Groq
    response = client.chat.completions.create(
        model=model,
        messages=[
            {
                "role": "user",
                "content": prompt
            }
        ],
        temperature=0,
        max_tokens=800
    )

    # استخراج الإجابة
    answer = response.choices[0].message.content

    # حماية إضافية في حالة رجوع None
    if not answer:
        answer = (
            "المعلومات المتاحة لا تكفي للإجابة على هذا "
            "السؤال بدقة."
        )

    return {
        "answer": answer.strip(),
        "disclaimer": DISCLAIMER
    }
```
